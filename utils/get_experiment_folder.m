function [folder, log] = get_experiment_folder(exp_name)
    % function [folder, log] = get_experiment_folder(exp_name)
    folder = create_time_stamped_folder(...
                fullfile('..', 'results'), ...
                exp_name, ...
                true, ...
                false);
    if nargout > 1
        log = fopen(fullfile(folder, 'log.txt'), 'w');
    end
end